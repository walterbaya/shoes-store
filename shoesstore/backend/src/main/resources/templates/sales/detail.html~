<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalle de Venta</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        :root {
            --primary-color: #4361ee;
            --secondary-color: #3f37c9;
            --success-color: #4cc9f0;
            --warning-color: #ff9f43;
            --danger-color: #ef476f;
            --info-color: #6f72b9;
            --light-bg: #f5f7fb;
            --card-shadow: 0 0.5rem 1.5rem rgba(0, 0, 0, 0.08);
        }

        body {
            background-color: var(--light-bg);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            padding: 1rem;
        }

        .order-card {
            transition: all 0.3s ease;
            border: none;
            border-radius: 0.75rem;
            overflow: hidden;
            box-shadow: var(--card-shadow);
            margin-bottom: 1.5rem;
        }

        .card-header {
            border-radius: 0.75rem 0.75rem 0 0 !important;
            padding: 1.5rem;
        }

        .channel-badge {
            padding: 0.4rem 0.8rem;
            border-radius: 50px;
            font-size: 0.85rem;
            font-weight: 500;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .badge-online {
            background-color: rgba(76, 201, 240, 0.15);
            color: #4cc9f0;
        }

        .badge-store {
            background-color: rgba(255, 159, 67, 0.15);
            color: #ff9f43;
        }

        .summary-card {
            border-radius: 0.75rem;
            border: none;
            box-shadow: var(--card-shadow);
            transition: all 0.3s ease;
            height: 100%;
        }

        .icon-circle {
            width: 44px;
            height: 44px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .bg-primary-light {
            background-color: rgba(67, 97, 238, 0.15);
        }

        .bg-success-light {
            background-color: rgba(76, 201, 240, 0.15);
        }

        .bg-warning-light {
            background-color: rgba(255, 159, 67, 0.15);
        }

        .bg-info-light {
            background-color: rgba(111, 114, 185, 0.15);
        }

        .bg-danger-light {
            background-color: rgba(239, 71, 111, 0.15);
        }

        .sale-detail-section {
            background: white;
            border-radius: 0.75rem;
            padding: 1.5rem;
            box-shadow: var(--card-shadow);
            margin-bottom: 1.5rem;
        }

        .product-img {
            width: 60px;
            height: 60px;
            object-fit: cover;
            border-radius: 0.5rem;
            background-color: #e9ecef;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            color: #6c757d;
        }

        .product-row {
            transition: background-color 0.2s;
        }

        .product-row:hover {
            background-color: rgba(67, 97, 238, 0.05);
        }

        .summary-item {
            display: flex;
            justify-content: space-between;
            padding: 0.75rem 0;
            border-bottom: 1px dashed #dee2e6;
        }

        .summary-total {
            font-weight: 700;
            font-size: 1.1rem;
            color: var(--primary-color);
        }

        .action-btn {
            width: 36px;
            height: 36px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border-radius: 50%;
            margin: 0 3px;
            transition: all 0.2s;
        }

        .print-btn {
            background-color: rgba(111, 114, 185, 0.1);
            color: var(--info-color);
        }

        .print-btn:hover {
            background-color: var(--info-color);
            color: white;
        }

        .back-btn {
            background-color: rgba(108, 117, 125, 0.1);
            color: #6c757d;
        }

        .back-btn:hover {
            background-color: #6c757d;
            color: white;
        }

        .section-title {
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--primary-color);
            margin-bottom: 1.25rem;
            padding-bottom: 0.5rem;
            border-bottom: 2px solid rgba(67, 97, 238, 0.2);
        }

        .payment-method {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            background-color: rgba(67, 97, 238, 0.05);
            border-radius: 0.5rem;
        }

        .payment-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background-color: rgba(67, 97, 238, 0.15);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--primary-color);
            font-size: 1.2rem;
        }

        .status-badge {
            padding: 0.4rem 0.8rem;
            border-radius: 50px;
            font-size: 0.85rem;
            font-weight: 500;
        }

        .status-completed {
            background-color: rgba(76, 201, 240, 0.15);
            color: #4cc9f0;
        }

        .status-pending {
            background-color: rgba(255, 159, 67, 0.15);
            color: #ff9f43;
        }

        .status-cancelled {
            background-color: rgba(239, 71, 111, 0.15);
            color: #ef476f;
        }

        @media print {
            body * {
                visibility: hidden;
            }
            .print-section, .print-section * {
                visibility: visible;
            }
            .print-section {
                position: absolute;
                left: 0;
                top: 0;
                width: 100%;
            }
            .no-print {
                display: none !important;
            }
        }
    </style>
</head>
<body>
<div class="container">
    <div class="row">
        <div class="col-12">
            <div class="card order-card print-section">
                <div class="card-header bg-primary text-white">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h1 class="h4 mb-0">
                                <i class="bi bi-receipt me-2" th:text="'Detalle de Venta #' + ${sale.id}"></i>
                            </h1>
                            <div class="d-flex align-items-center mt-2">
                                <span class="channel-badge badge-online">
                                    <i class="bi bi-globe me-1"></i> <span th:text="${sale.channel.name == 'ONLINE' ? 'Venta Online' : 'Venta en Tienda'}"></span>
                                </span>
                                <span class="ms-3 text-light"><i class="bi bi-calendar me-1"></i> <span th:text="${#temporals.format(sale.saleDate, 'dd MMMM yyyy, HH:mm')}"></span></span>
                            </div>
                        </div>
                        <div class="d-flex">
                            <a href="javascript:window.print()" class="action-btn print-btn me-2" title="Imprimir">
                                <i class="bi bi-printer"></i>
                            </a>
                            <a href="/sales" class="action-btn back-btn" title="Volver">
                                <i class="bi bi-arrow-left"></i>
                            </a>
                        </div>
                    </div>
                </div>

                <div class="card-body">
                    <!-- Información del cliente -->
                    <div class="row mb-4">
                        <div class="col-lg-4">
                            <div class="sale-detail-section">
                                <h5 class="section-title"><i class="bi bi-receipt me-2"></i>Resumen de la Venta</h5>
                                <div class="payment-method mb-3">
                                    <div class="payment-icon">
                                        <i class="bi" th:class="${sale.paymentMethod.name == 'TARJETA' ? 'bi-credit-card' : sale.paymentMethod.name == 'EFECTIVO' ? 'bi-cash' : 'bi-bank'}"></i>
                                    </div>
                                    <div>
                                        <div class="fw-medium" th:text="${sale.paymentMethod.name}"></div>
                                    </div>
                                </div>

                                <div class="summary-item">
                                    <span>Porcentaje de Descuento:</span>
                                    <span th:text="${#numbers.formatDecimal(sale.discountPercentage, 1, 2) + ' %'}"></span>
                                </div>
                                <div class="summary-item"><span>Descuento:</span> <span th:text="${'$' + #numbers.formatDecimal(sale.total * (sale.discountPercentage / 100), 1, 2)}"></span></div>
                                <div class="summary-item">
                                    <span>Envío:</span>
                                    <span th:text="${'$' + #numbers.formatDecimal(sale.shippingCost, 1, 2)}"></span>
                                </div>
                                <div class="summary-item">
                                    <span>IVA (21%):</span>
                                    <span th:text="${'$' + #numbers.formatDecimal(sale.total * 0.21, 1, 2)}"></span>
                                </div>
                                <div class="summary-item mb-3">
                                    <span class="summary-total">Total:</span>
                                    <span class="summary-total" th:text="${'$' + #numbers.formatDecimal(sale.total, 1, 2)}"></span>
                                </div>
                                <div class="d-grid mt-3">
                                    <button class="btn btn-primary no-print">
                                        <i class="bi bi-download me-1"></i> Descargar Factura
                                    </button>
                                </div>
                            </div>
                        </div>

                        <div class="sale-detail-section mb-4 col-8">
                            <h5 class="section-title"><i class="bi bi-cart me-2"></i>Productos</h5>
                            <div class="table-responsive">
                                <table class="table align-middle">
                                    <thead>
                                    <tr>
                                        <th>ID </th>
                                        <th>Tipo</th>
                                        <th>Marca</th>
                                        <th>Descripción </th>
                                        <th>Talle</th>
                                        <th>Genero</th>
                                        <th class="text-center">Precio Unitario</th>
                                        <th class="text-center">Cantidad</th>
                                        <th class="text-end">Subtotal</th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    <tr th:each="sp : ${products}" class="product-row">
                                        <td th:text="${sp.product.id}"></td>
                                        <td th:text="${sp.product.type}"></td>
                                        <td th:text="${sp.product.brand}"></td>
                                        <td th:text="${sp.product.description}"></td>
                                        <td th:text="${sp.product.size.displayValue}"></td>
                                        <td class="text-center" th:text="${'$' + #numbers.formatDecimal(sp.product.price,1,2)}"></td>
                                        <td class="text-center" th:text="${sp.quantity}"></td>
                                        <td class="text-end fw-medium" th:text="${'$' + #numbers.formatDecimal(sp.subtotal, 1, 2)}"></td>
                                    </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Inicializar tooltips
        const tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
        tooltipTriggerList.map(function (tooltipTriggerEl) {
            return new bootstrap.Tooltip(tooltipTriggerEl);
        });

        // Simular datos dinámicos
        const saleData = {
            id: "VS-2023-00158",
            date: "15/07/2023 14:35",
            status: "Completada",
            channel: "ONLINE",
            customer: {
                name: "María Rodríguez",
                email: "maria.rodriguez@example.com",
                phone: "+34 612 345 678",
                address: "Calle Gran Vía, 28, 3ºB, 28013 Madrid, España"
            },
            payment: {
                method: "Tarjeta de Crédito",
                type: "Visa",
                lastDigits: "4242",
                auth: "AUTH-789456",
                status: "Completado"
            },
            shipping: {
                carrier: "Correos Express",
                tracking: "ES345678901234567890",
                status: "Entregado",
                deliveryDate: "17/07/2023"
            },
            products: [
                { name: "Portátil UltraSlim Pro", ref: "LT-2023-001", price: 1299.00, quantity: 1, subtotal: 1299.00 },
                { name: "Ratón Inalámbrico Ergonómico", ref: "AC-2023-045", price: 45.90, quantity: 2, subtotal: 91.80 },
                { name: "Disco Duro Externo 1TB", ref: "AC-2023-128", price: 89.99, quantity: 1, subtotal: 89.99 }
            ],
            summary: {
                subtotal: 1480.79,
                discount: 74.04,
                shipping: 9.99,
                tax: 297.40,
                total: 1714.14
            }
        };

        // En un entorno real, estos datos vendrían del backend a través de Thymeleaf
        console.log("Datos de la venta:", saleData);
    });
</script>
</body>
</html>
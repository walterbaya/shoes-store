<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Nuevo Proveedor</title>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-4">
    <div class="card shadow">
        <div class="card-header bg-primary text-white">
            <h2 class="mb-0">Registrar Nuevo Proveedor</h2>
        </div>
        <div class="card-body">
            <form th:action="@{${supplier.id} != null ? '/suppliers/update/' + ${supplier.id} : '/suppliers/save'}" th:object="${supplier}" method="post">
                <!-- Campo oculto para ID en caso de edición -->
                <input type="hidden" th:if="${supplier?.id}" th:field="*{id}"/>

                <div class="mb-3">
                    <label for="name" class="form-label fw-bold">Nombre del Proveedor</label>
                    <input type="text" class="form-control" id="name" th:field="*{name}" required>
                </div>

                <div class="mb-3">
                    <label for="email" class="form-label fw-bold">Email del Proveedor</label>
                    <input type="email" class="form-control" id="email" th:field="*{email}" required>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold">Productos Asociados</label>
                    <div class="form-check" th:each="product : ${products}">
                        <input class="form-check-input"
                               type="checkbox"
                               th:id="'product_' + ${product.id}"
                               th:name="productIds"
                               th:value="${product.id}"
                               th:checked="${supplier != null and supplier.products.contains(product)}" />
                        <label class="form-check-label" th:for="'product_' + ${product.id}"
                               th:text="${product.name}">Nombre del Producto</label>
                    </div>
                </div>

                <button type="submit" class="btn btn-success">Guardar</button>
                <a th:href="@{/suppliers}" class="btn btn-secondary">Cancelar</a>
            </form>
        </div>
    </div>
</div>
</body>
</html>


<div th:fragment="content card shadow">
    <div class="card-header bg-primary text-white">
        <div class="d-flex justify-content-between align-items-center">
            <h1 class="h4 mb-0">Gestión de Proveedores</h1>
            <span class="badge bg-light text-primary" th:text="${suppliers.size()} + ' proveedores'"></span>
        </div>
    </div>

    <div class="card-body">
        <div class="d-flex flex-wrap justify-content-between align-items-center mb-4">
            <div class="search-container" style="max-width: 400px;">
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-search"></i></span>
                    <input type="text" id="supplierSearch" class="form-control" placeholder="Buscar proveedor...">
                </div>
            </div>
            <a th:href="@{/suppliers/new}" class="btn btn-primary btn-lg mt-4"><i class="bi bi-plus-circle me-2s"></i>Registrar Nuevo Proveedor</a>
        </div>

        <div class="table-responsive">
            <table class="table table-hover align-middle">
                <thead class="table-light">
                <tr>
                    <th class="py-3">ID</th>
                    <th class="py-3">Nombre</th>
                    <th class="py-3">Contacto</th>
                    <th class="py-3">CUIT/CUIL</th>
                    <th class="py-3">Telefono</th>
                    <th class="py-3">Estado</th>
                    <th class="py-3">Productos</th>
                    <th class="py-3">Condiciones de Pago</th>
                    <th class="py-3">Acciones</th>
                </tr>
                </thead>
                <tbody>
                <tr th:each="supplier : ${suppliers}">
                    <td th:text="${supplier.id}">1</td>
                    <td>
                        <div class="fw-medium" th:text="${supplier.name}">Proveedor X</div>
                        <div class="text-muted small" th:text="${supplier.email}">proveedor@ejemplo.com</div>
                    </td>
                    <td th:text="${supplier.contactName}">30-12345678-9</td>
                    <td th:text="${supplier.cuit}">30-12345678-9</td>
                    <td>
                        <div th:text="${supplier.phone} ?: 'Sin teléfono'">+54 11 1234-5678</div>
                        <div class="text-muted small" th:text="${supplier.address} ?: 'Sin dirección'">Calle Falsa 123</div>
                    </td>
                    <td>
                        <span th:if="${supplier.active}" class="badge bg-success">Activo</span>
                        <span th:unless="${supplier.active}" class="badge bg-danger">Inactivo</span>
                    </td>
                    <td>
                        <div th:if="${not supplier.products.empty}">
                            <span class="badge bg-primary rounded-pill" th:text="${supplier.products.size()}"></span>
                            <span>productos</span>
                        </div>
                        <span th:if="${supplier.products.empty}" class="text-muted">Sin productos</span>
                    </td>
                    <td>
                        <div class="d-flex flex-wrap gap-1">
                            <span th:each="condition : ${supplier.paymentConditions}"
                                  class="badge bg-light text-dark border"
                                  th:text="${condition.displayName}">
                                Efectivo
                            </span>
                            <span th:if="${supplier.paymentConditions.empty}" class="text-muted">No especificado</span>
                        </div>
                    </td>
                    <td>
                        <div class="d-flex gap-1">
                            <a th:href="@{/suppliers/update/{id}(id=${supplier.id})}"
                               class="btn btn-sm btn-warning">
                                <i class="bi bi-pencil"></i>
                            </a>
                            <form method="post"
                                  th:action="@{'/suppliers/delete/' + ${supplier.id}}"
                                  style="display:inline-block"
                                  >
                                <input type="hidden" name="_method" value="delete">
                                <button type="submit"
                                        class="action-btn btn btn-sm btn-danger"
                                        title="Eliminar"
                                        data-bs-toggle="tooltip"
                                        onclick="return confirm('¿Está seguro de eliminar este proveedor?');">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </form>

                        </div>
                    </td>
                </tr>
                <tr th:if="${suppliers.empty}">
                    <td colspan="8" class="text-center py-4">
                        <div class="d-flex flex-column align-items-center">
                            <i class="bi bi-building-gear display-4 text-muted mb-3"></i>
                            <h4 class="mb-2">No hay proveedores registrados</h4>
                            <p class="text-muted">Comience agregando un nuevo proveedor</p>
                            <a th:href="@{/suppliers/new}" class="btn btn-primary mt-2">
                                <i class="bi bi-plus-circle me-2"></i>Agregar Proveedor
                            </a>
                        </div>
                    </td>
                </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>

<script>
    // Función de búsqueda en tiempo real
    document.getElementById('supplierSearch').addEventListener('input', function() {
        const filter = this.value.toLowerCase();
        const rows = document.querySelectorAll('tbody tr');
        let visibleCount = 0;

        rows.forEach(row => {
            if (row.querySelector('td')) {
                const text = row.textContent.toLowerCase();
                if (text.includes(filter)) {
                    row.style.display = '';
                    visibleCount++;
                } else {
                    row.style.display = 'none';
                }
            }
        });

        // Actualizar contador
        document.querySelector('.badge.bg-light.text-primary').textContent =
            visibleCount + ' proveedores';
    });
</script>

<style>
    .card.shadow {
        border-radius: 0.75rem;
        transition: transform 0.2s ease-in-out;
    }

    .card.shadow:hover {
        transform: translateY(-3px);
        box-shadow: 0 0.5rem 1.5rem rgba(0, 0, 0, 0.15) !important;
    }

    .card-header.bg-primary {
        border-radius: 0.75rem 0.75rem 0 0;
        padding: 1.25rem 1.5rem;
    }

    .card-title {
        font-size: 1.1rem;
        font-weight: 500;
        color: rgba(255, 255, 255, 0.9);
    }

    .card-text.display-5 {
        font-size: 1.75rem;
        font-weight: 600;
        letter-spacing: -0.5px;
    }

    #salesChart {
        border-radius: 0.5rem;
        background: #fff;
    }

    /* Mejoras para la tabla */
    .table-hover tbody tr {
        transition: all 0.2s ease;
    }

    .table-hover tbody tr:hover {
        background-color: rgba(67, 97, 238, 0.05);
    }

    .search-container .input-group-text {
        background: white;
        border-right: none;
    }

    .search-container .form-control {
        border-left: none;
    }
</style>